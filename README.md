# 엠제이 운영 관리 · 공유용 데모

Figma v3의 MJE 보라색과 shadcn/ui 구성을 실제 React 화면으로 구현한 체험 사이트입니다.

## 실행

```sh
npm ci
npm run dev
```

## 구현한 기능

- 학사 일정: 월간/12개월 보기, 기간 일정, 분류·학교·제목 필터, 등록·수정·삭제, 모바일 날짜별 목록.
- 토요 보강: 교시별/전체 목록/학습 시간/완료/날짜 미정 보기, 토요일 및 30분 단위 검증, 중복 예약 검사, 출석·학습 결과, 주간 보관 후 보강만 초기화, 인쇄.
- 상담: 주간 목록/전체 이력, 유형·담당자·검색 필터, 신규 대상 및 재원생 전환, 예약·취소·미방문, 결과 임시 저장/완료, 대상자별 누적 기록, 후속 예약, 수정 이력.
- 공통: 브라우저별 데모 저장, 다른 탭 갱신, JSON 내보내기, 샘플 초기화, 모바일 메뉴와 편집 패널.

## 저장 경계

가상 학생과 가상 상담 기록만 포함합니다. 실제 DB·로그인·직원 권한·다른 기기 동기화는 없습니다. `localStorage`의 `mje-academy-demo-v1` 키에 해당 브라우저의 수정 내용만 저장합니다. 초기화하면 해당 브라우저 데이터만 샘플로 바뀝니다. 실제 개인정보는 입력하지 마세요.

`lib/demo-repository.ts`를 향후 인증된 API 어댑터로 교체합니다. 현재 입력 검증은 클라이언트 체험용이며, 운영 전 동일 검증 및 권한·경합 방지를 서버에서 구현해야 합니다. 정기 유형은 초기화 시 유지하지만 다음 주 자동 편성은 구현하지 않았습니다.

## 검증

```sh
npx tsc --noEmit
node --experimental-strip-types --test tests/domain.test.ts
```

배포는 Sites에 연결되어 있으며 `.openai/hosting.json`은 DB 바인딩 없이 설정했습니다. 실제 DB 연결 및 비용 결정은 후속 단계입니다.

## 화면 개선 (2026-09-21)

- 달력 일정에 마우스를 올리거나 키보드 포커스를 두면 제목·기간·시간·담당자·장소·메모를 요약 표시합니다.
- 토요 보강은 하루에 교사 한 명이 담당합니다. 상단과 학생 상세는 날짜별 로테이션에서 동일한 담당 교사를 표시합니다.
- ‘보강 스케줄’에서 시작 토요일과 교사 순서를 설정하면 매주 한 명씩 순환 배정합니다. 위아래 버튼으로 순서를 조정하고 8주 배정을 미리 봅니다. 새 규칙의 시작일 이전 배정과 보관 기록은 유지합니다.
- 상담 예약과 결과 편집은 중앙 대화상자로 열립니다. 예약(파랑), 완료(초록), 취소(보라), 미방문(빨강), 결과 미기록(주황)을 구분합니다.
- 사이드바의 학년도·운영 시간 문구를 제거했습니다.
- 기존 데모는 초기화하지 않고 주간 로테이션 모델로 전환합니다. 예전의 다중 교사 배치는 가장 이른 배치의 교사 순서를 첫 로테이션으로 사용합니다.
- ‘바로 상담 일지 작성’에서 예약 없이 대상·일시·상담 내용을 입력하고 임시 저장/완료합니다. 직접 작성한 기록은 예약 시간 충돌 검사에서 제외하고 작성 중 상태로 구별합니다.
- 학사 일정 담당자는 선택 사항이며 ‘미지정’으로 해제할 수 있습니다.

## 당일 대체 담당과 선생님 등록

- 토요 보강 상단의 ‘당일 담당 변경’에서 특정 토요일만 다른 교사로 지정합니다. ‘기본 로테이션 사용’을 선택하면 복원됩니다. 이후 주차의 순환과 기존 보관 기록은 유지됩니다.
- 보강 스케줄의 ‘선생님 등록’에서 이름을 추가합니다. 등록은 로테이션 자동 참여와 별개이며, 참여 선생님에 추가 후 순서를 저장하거나 당일 대체로 선택합니다.
- 등록한 선생님은 학사 일정과 상담 담당자 선택에서도 사용합니다. 중복 이름은 차단하며 동명이인은 구분 가능한 이름으로 등록합니다.
- 기존 브라우저 데이터를 유지하며 선생님 목록과 날짜별 대체 배정을 추가합니다. 실제 직원 계정이나 로그인 권한을 생성하는 기능은 아닙니다.
